# Generated by .brand/gen_repo_compose.py — regenerate rather than hand-edit.
#
#   infisical export --projectId techyst-14-onyx --env prod --format dotenv > .env
#   docker compose up -d
#
# Images come from ghcr.io; `docker login ghcr.io` first. Databases, cache,
# queue and object storage are all external, so nothing here holds state and
# `docker compose down` is safe.

name: techyst-14-onyx
services:
  api_server:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-onyx-api_server:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    command:
    - /bin/sh
    - -c
    - alembic upgrade head && uvicorn onyx.main:app --host 0.0.0.0 --port 8080
    ports:
    - 8085:8080
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  background:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-onyx-background:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    command:
    - /usr/bin/supervisord
    - -c
    - /etc/supervisor/conf.d/supervisord.conf
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  inference_model_server:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-onyx-inference_model_server:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    command:
    - uvicorn
    - model_server.main:app
    - --host
    - 0.0.0.0
    - --port
    - '9000'
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  indexing_model_server:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-onyx-indexing_model_server:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    command:
    - uvicorn
    - model_server.main:app
    - --host
    - 0.0.0.0
    - --port
    - '9000'
    - --limit-concurrency
    - '10'
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
