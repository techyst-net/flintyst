# onyx-cnpg-crds ships only the CloudNativePG CRDs (crds/cnpg-crds.yaml) and has
# no configurable values. The parent onyx chart gates it via the
# postgresql.installCRDs / postgresql.enabled condition in its Chart.yaml.
# This file exists because chart-testing (ct lint) requires a values.yaml.
